build_script:
  - ghc Main.hs -O2
  - gcc compiler.c -o transpiler.exe -O3
#

before_build:
  - choco install ghc
  - refreshenv
  - ghci --version
#

artifacts:
  - path: Main.exe
    name: Main
  - path: transpiler.exe
    name: transpiler
#
